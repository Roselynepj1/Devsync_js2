import { setCurrentUserMenuAvatar } from "./utilities/events.mjs"

document.addEventListener('DOMContentLoaded',()=>{
    //check the user if logged in
    //set the event details
    //find the avatar details and update them
    setCurrentUserMenuAvatar()
})